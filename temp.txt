.smaller.social-icons-container {
    margin: 0 auto 2em;
    display: flex;
    justify-content: space-around;
    width: 80%;
    max-width: 350px;
  }
  
  .smaller a.social-icon:link,
  .smaller a.social-icon:visited,
  .smaller a.social-icon:focus {
    border: 2px solid white;
    border-radius: 50%;
    width: 3em;
    height: 3em;
    display: flex;
    justify-content: center;
    align-items: center;
    background: none;
  }
  
  .smaller.social-icon:active {
    cursor: pointer;
    background: var(--dark);
    color: var(--light);
    border: 2px solid var(--dark);
  }
  
  .smaller .fa-brands,
  .smaller .fa-solid,
  .smaller .fa-regular {
    font-size: 1.5rem;
    color: #fff;
    background: none;
    border-radius: 50%;
    padding: 0;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .smaller .fa-brands:hover,
  .smaller .fa-solid:hover,
  .smaller .fa-regular:hover,
  .smaller .fa-brands:active,
  .smaller .fa-solid:active,
  .smaller .fa-regular:active {
    background: var(--dark);
    color: var(--light);
  }
  


  ------------------------- LARGER

    .larger .social-icons-container {
    justify-content: flex-start;
  }

  .larger .social-icon:link,
  .larger .social-icon:visited,
  .larger .social-icon:focus {
    margin-right: 1.5em;
    width: 3.5em;
    height: 3.5em;
    transition: all 0.35s;
  }

  .larger.social-icon:hover,
  .larger.social-icon:active {
    transform: translateY(-10px);
    background: var(--light);
    color: var(--dark);
  }

  .larger.fa-brands,
  .larger.fa-solid,
  .larger.fa-regular {
    font-size: 1.5rem;
  }

  .column-two-container {
    width: 60%;
  }